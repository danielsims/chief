-- Added idempotently by ensureChannelManagementSchema. Keeping the generated
-- snapshot makes future schema diffs accurate without replaying columns that
-- pre-release desktop builds may already have installed.
SELECT 1;
