import type { AgentManifest } from "../manifest.js";

export const setup = {
  id: "setup",
  name: "Setup",
  role: "Growth Systems Setup",
  description:
    "Connects marketing integrations and prepares narrowly scoped, reviewable measurement changes.",
} satisfies AgentManifest;
