{
  "name": "@chief/executor",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --target=node24 --format=esm --external:ws --outfile=dist/server.mjs",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "tsx watch src/server.ts",
    "format": "prettier --check . --ignore-path ../../.gitignore --ignore-path ../../.prettierignore",
    "lint": "pnpm --dir ../.. exec oxlint apps/executor",
    "start": "node dist/server.mjs",
    "test": "node --import tsx --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@chief/browser": "workspace:*",
    "@chief/relay-contracts": "workspace:*",
    "agent-browser": "0.32.3",
    "jose": "^6.2.3",
    "ws": "^8.18.3",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@chief/prettier-config": "workspace:*",
    "@chief/tsconfig": "workspace:*",
    "@types/node": "catalog:",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.0",
    "prettier": "catalog:",
    "tsx": "^4.20.3",
    "typescript": "catalog:"
  },
  "prettier": "@chief/prettier-config"
}
